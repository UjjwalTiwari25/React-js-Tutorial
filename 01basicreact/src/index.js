import React from 'react';
import ReactDOM from 'react-dom/client';

import App from './App';


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />   
                            {/* we can make custom tags....react gives us the power of JSX..mtlb hm javascript k  through html element ko render kr sakte hai */}
  
  </React.StrictMode>
);
